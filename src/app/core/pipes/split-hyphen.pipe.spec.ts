import { SplitHyphenPipe } from './split-hyphen.pipe';

describe('SplitHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
